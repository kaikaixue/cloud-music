<!--components/playlist/playlist.wxml-->
<view class="playlist-container">
  <image src="{{playlist.priUrl}}" class="playlist-img"></image>
  <text class="playlist-playcount">{{_count}}</text>
  <view class="playlist-name">{{playlist.name}}</view>
</view>
